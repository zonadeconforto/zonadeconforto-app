# Zona de Conforto

## Getting Started

Para executar o projeto sem o docker, execute o comando a seguir:

```bash
pnpm install
```

E depois execute o projeto:

```bash
pnpm dev
```

Abra [http://localhost:3000](http://localhost:3000) no seu browser.
